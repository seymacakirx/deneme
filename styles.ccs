:root{
  --bg:#ffffff;                 /* beyaz arka plan */
  --fg:#1a1a1a;                /* koyu yazı */
  --muted:#555555;

  --card:rgba(158,27,50,.05);  /* çok hafif kırmızı ton */
  --border:rgba(158,27,50,.15);

  --accent:#9E1B32;            /* Latvia kırmızısı */
  --accent2:#C62839;           /* ikinci kırmızı ton */

  --shadow: 0 12px 30px rgba(0,0,0,.10);
  --radius:16px;
}

*{ box-sizing:border-box; }

html{ scroll-behavior:smooth; }

body{
  margin:0;
  font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial;
  background:var(--bg);
  color:var(--fg);
  line-height:1.55;
}

a{ color:inherit; text-decoration:none; }

.container{
  width:min(1100px, calc(100% - 40px));
  margin:0 auto;
}

/* TOPBAR */
.topbar{
  position:fixed;
  top:0; left:0; right:0;
  z-index:50;
  background: rgba(255,255,255,.85);
  border-bottom:1px solid rgba(0,0,0,.08);
  backdrop-filter: blur(10px);
}

.topbar__inner{
  display:flex;
  align-items:center;
  justify-content:space-between;
  height:76px;
  gap:16px;
}

.brand__logo{
  height:120px;
  width:auto;
  display:block;
}

.nav{
  display:flex;
  gap:22px;
  align-items:center;
}

.nav a{
  font-weight:600;
  color: var(--fg);
  padding:10px 8px;
  border-radius:10px;
}

.nav a:hover{
  background: rgba(158,27,50,.08);
}

.nav__toggle{
  display:none;
  width:44px;
  height:44px;
  border:1px solid rgba(0,0,0,.12);
  background: rgba(255,255,255,.8);
  border-radius:12px;
  box-shadow: var(--shadow);
  cursor:pointer;
}

.nav__toggle span{
  display:block;
  height:2px;
  width:20px;
  background: var(--fg);
  margin:5px auto;
  border-radius:2px;
}

/* HERO */
.hero{
  position:relative;
  min-height:100vh;
  display:flex;
  align-items:center;
  overflow:hidden;
  padding-top:76px;
}

.hero__video{
  position:absolute;
  inset:0;
  width:100%;
  height:100%;
  object-fit:cover;
}

.hero__overlay{
  position:absolute;
  inset:0;
  background: rgba(255,255,255,.55); /* açık overlay */
}

.hero__content{
  position:relative;
  z-index:2;
  padding:68px 0 80px;
}

.hero h1{
  font-size: clamp(34px, 5vw, 56px);
  margin:0 0 10px;
}

.hero p{
  margin:0 0 22px;
  font-size: clamp(18px, 2.5vw, 24px);
  font-weight:600;
  color: rgba(0,0,0,.75);
  max-width: 56ch;
}

/* BUTTONS */
.btn{
  display:inline-flex;
  align-items:center;
  justify-content:center;
  gap:10px;
  padding:12px 16px;
  border-radius:14px;
  border:1px solid transparent;
  font-weight:700;
  cursor:pointer;
}

.btn--primary{
  background: linear-gradient(135deg, var(--accent), var(--accent2));
  color:white;
}

.btn--primary:hover{ filter: brightness(1.05); }

.btn--ghost{
  background: rgba(158,27,50,.08);
  border-color: rgba(158,27,50,.2);
}

/* BADGES */
.badge{
  padding:8px 12px;
  border-radius:999px;
  background: rgba(158,27,50,.08);
  border:1px solid rgba(158,27,50,.15);
  font-weight:600;
}

/* SECTIONS */
.section{
  padding:72px 0;
  border-top:1px solid rgba(0,0,0,.06);
}

.section--alt{
  background: rgba(158,27,50,.03);
}

h2{ font-size:30px; margin:0 0 10px; }

.lead{
  margin:0 0 26px;
  color: var(--muted);
}

/* CARDS */
.cards{
  display:grid;
  grid-template-columns: repeat(3, 1fr);
  gap:18px;
}

.card{
  background:white;
  border:1px solid rgba(158,27,50,.15);
  border-radius: var(--radius);
  padding:18px;
  box-shadow: var(--shadow);
}

/* FORM */
input, textarea{
  width:100%;
  margin-top:8px;
  background:white;
  border:1px solid rgba(158,27,50,.25);
  border-radius:12px;
  padding:12px;
  color:var(--fg);
}

input:focus, textarea:focus{
  border-color: var(--accent);
  box-shadow: 0 0 0 3px rgba(158,27,50,.15);
}

/* FOOTER */
.footer{
  padding:26px 0;
  border-top:1px solid rgba(0,0,0,.06);
  color: var(--muted);
}

/* MOBILE */
@media (max-width:900px){
  .cards{ grid-template-columns:1fr; }
}

@media (max-width:720px){
  .nav{ display:none; }

  .nav__toggle{ display:inline-block; }

  .nav.is-open{
    display:flex;
    position:absolute;
    top:76px;
    right:20px;
    flex-direction:column;
    background:white;
    border:1px solid rgba(0,0,0,.1);
    border-radius:16px;
    padding:10px;
  }

  .brand__logo{ height:90px; }
}
